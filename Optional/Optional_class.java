// Optional is a class
// they are used to avoid NullPointerExceptions
import java.util.Optional;

class Optional_class{
	public static void main(String args[]){
		String[] words = new String[5];
		// static factory methods are used to create objects when we don't want to expose constructors they are private, of is a static method that's why we are directly accessing it using the class name and we don't have to create an object for it, internally it creates an object	
//		Optional<String> var_name = Optional.ofNullable(words[

//		if(var_name.isPresent()){
//		String word = words[4].toLowerCase();
//		System.out.println(word);
//		}
//		else{
//			System.out.println("not found niga");
//		}
	}
}
